apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: pvc-patcher-binding
  namespace: ${namespace}
subjects:
  - kind: ServiceAccount
    name: pvc-patcher-sa
    namespace: ${namespace}
roleRef:
  kind: Role
  name: pvc-patcher-role
  apiGroup: rbac.authorization.k8s.io